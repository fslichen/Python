import Function
import random
# Use namespace.functionName() to call a function within a certain module.
Function.function()
# You can download a package from the internet and include them into python or import the python built-in packages.
x = random.randrange(1, 100)
print(x)
